@* @model LibraryManagementSystem.Models.Books.EditBookViewModel

@{
    ViewData["Title"] = "Edit Book";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

<div class="container my-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white rounded-top-4">
            <h3 class="mb-0"><i class="fas fa-book-open me-2"></i>Edit Book</h3>
        </div>
        <div class="card-body bg-light">
            @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string))
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["ErrorMessage"]
                </div>
            }

            <form asp-action="EditBook" asp-controller="Librarian" method="post" enctype="multipart/form-data">
                <input type="hidden" name="BookId" value="@Model.BookId" />

                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Title</label>
                        <input type="text" class="form-control" name="Title" value="@Model.Title" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Author</label>
                        <input type="text" class="form-control" name="Author" value="@Model.Author" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">ISBN</label>
                        <input type="text" class="form-control" name="ISBN" value="@Model.ISBN" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Price</label>
                        <input type="number" class="form-control" name="Price" value="@Model.Price" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Published Date</label>
                        <input type="date" class="form-control" name="PublishedDate" value="@Model.PublishedDate.ToString("yyyy-MM-dd")" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Genre</label>
                        <input type="text" class="form-control" name="Genre" value="@Model.Genre" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Category</label>
                        <input type="text" class="form-control" name="Category" value="@Model.Category" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Subject</label>
                        <input type="text" class="form-control" name="Subject" value="@Model.Subject" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Condition</label>
                        <input type="text" class="form-control" name="Condition" value="@Model.Condition" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Total Copies</label>
                        <input type="number" class="form-control" name="TotalCopies" value="@Model.TotalCopies" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Language</label>
                        <input type="text" class="form-control" name="Language" value="@Model.Language" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Edition</label>
                        <input type="text" class="form-control" name="Edition" value="@Model.Edition" />
                    </div>

                    <div class="col-12">
                        <label class="form-label">Summary</label>
                        <textarea class="form-control" name="Summary" rows="3">@Model.Summary</textarea>
                    </div>

                    <div class="col-12">
                        <label class="form-label">Upload New Book Image</label>
                        <input type="file" class="form-control" name="BookImage" accept="image/*" />
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ImagePath))
                    {
                        <div class="col-12">
                            <label class="form-label">Current Image:</label><br />
                            <img src="@Model.ImagePath" alt="Book Image" class="img-thumbnail mt-1" style="max-height: 150px;" />
                        </div>
                    }
                </div>

                <div class="d-flex justify-content-end mt-4 gap-2">
                    <button type="submit" class="btn btn-success px-4"><i class="fas fa-save me-1"></i> Save Changes</button>
                    <a asp-action="Index" class="btn btn-outline-secondary px-4"><i class="fas fa-arrow-left me-1"></i> Back</a>
                </div>
            </form>
        </div>
    </div>
</div>
 *@

                            @model LibraryManagementSystem.Models.Books.EditBookViewModel

@{
    ViewData["Title"] = "Edit Book";
}

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

<div class="container my-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-primary text-white rounded-top-4">
            <h3 class="mb-0"><i class="fas fa-book-open me-2"></i>Edit Book</h3>
        </div>
        <div class="card-body bg-light">
            @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string))
            {
                <div class="alert alert-danger" role="alert">
                    @ViewData["ErrorMessage"]
                </div>
            }

            <form asp-action="EditBook" asp-controller="Librarian" method="post" enctype="multipart/form-data">
                <input type="hidden" name="BookId" value="@Model.BookId" />

                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Title</label>
                        <input type="text" class="form-control" name="Title" value="@Model.Title" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Author</label>
                        <input type="text" class="form-control" name="Author" value="@Model.Author" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">ISBN</label>
                        <input type="text" class="form-control" name="ISBN" value="@Model.ISBN" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Price</label>
                        <input type="number" class="form-control" name="Price" value="@Model.Price" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Published Date</label>
                        <input type="date" class="form-control" name="PublishedDate" value="@Model.PublishedDate.ToString("yyyy-MM-dd")" required />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Genre</label>
                        <input type="text" class="form-control" name="Genre" value="@Model.Genre" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Category</label>
                        <input type="text" class="form-control" name="Category" value="@Model.Category" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Subject</label>
                        <input type="text" class="form-control" name="Subject" value="@Model.Subject" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Condition</label>
                        <input type="text" class="form-control" name="Condition" value="@Model.Condition" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Total Copies</label>
                        <input type="number" class="form-control" name="TotalCopies" value="@Model.TotalCopies" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Language</label>
                        <input type="text" class="form-control" name="Language" value="@Model.Language" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Sell Book Quantity</label>
                        <input type="number" class="form-control" name="SellBook" value="@Model.SellBook" />
                    </div>

                    <div class="col-md-6">
                        <label class="form-label">Edition</label>
                        <input type="text" class="form-control" name="Edition" value="@Model.Edition" />
                    </div>

                    <div class="col-12">
                        <label class="form-label">Summary</label>
                        <textarea class="form-control" name="Summary" rows="3">@Model.Summary</textarea>
                    </div>

                    <div class="col-12">
                        <label class="form-label">Upload New Book Image</label>
                        <input type="file" class="form-control" name="BookImage" accept="image/*" />
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ImagePath))
                    {
                        <div class="col-12">
                            <label class="form-label">Current Image:</label><br />
                            <img src="@Model.ImagePath" alt="Book Image" class="img-thumbnail mt-1" style="max-height: 150px;" />
                        </div>
                    }
                </div>

                <div class="d-flex justify-content-end mt-4 gap-2">
                    <button type="submit" class="btn btn-success px-4"><i class="fas fa-save me-1"></i> Save Changes</button>
                    <a asp-action="Index" class="btn btn-outline-secondary px-4"><i class="fas fa-arrow-left me-1"></i> Back</a>
                </div>
            </form>
        </div>
    </div>
</div>
